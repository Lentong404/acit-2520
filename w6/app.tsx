// const express = require("express");
// http sits on top of the net module

//gives back a server
// const app = http.createServer();
//server is turned off by default 

//turn on
// app.listen();

//when this server is running somewhere, you may have more than one server